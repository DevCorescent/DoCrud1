import PublishedPage from '@/components/PublishedPage';

export const metadata = {
  title: 'Published | Docrud',
  description: 'Browse all published content — news, articles, documents, portfolios, announcements, job posts, resumes and products on Docrud.',
};

export default function Page() {
  return <PublishedPage />;
}
