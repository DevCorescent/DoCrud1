import RecentsPage from '@/components/RecentsPage';

export const metadata = {
  title: 'Recents',
  description: 'Stories from your network — browse, filter and discover recent posts.',
};

export default function Page() {
  return <RecentsPage />;
}
